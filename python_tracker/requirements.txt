# Visible Ephemeris — Python tracker + Qt dashboard dependencies.
#
# Verified on Python 3.12.3 (2026-07-29) and Python 3.14.4 (2026-05-25):
# venv builds clean and the Python/C++ equivalence test passes.
#
# Install:  python3 -m venv venv && ./venv/bin/pip install -r requirements.txt
#
# Direct dependencies only; transitive deps (sgp4, jplephem, starlette,
# pydantic, click, h11, anyio, certifi, urllib3, idna, charset-normalizer,
# PyQt6-Qt6, PyQt6-sip, contourpy, cycler, fonttools, kiwisolver, packaging,
# pillow, pyparsing, python-dateutil, etc.) are resolved automatically by pip.

# ------------------------------------------------------------------ tracker
skyfield==1.54
requests>=2.31
fastapi>=0.128
uvicorn>=0.40
PyYAML>=6.0
numpy>=2.4

# ------------------------------------------------------- Qt 6 dashboards
# Powers python_tracker/qt_dashboards/{ve_ide, orbital_architect_qt, hpop_panel}.
# 6.6 is the earliest release that matches our tested surface (QFileSystemModel
# in QtGui, QMenu.addAction signature). pyqtgraph 0.13 is the first release
# with first-class PyQt6 support.
PyQt6>=6.6
pyqtgraph>=0.13
matplotlib>=3.6

# --------------------------------------------------- optional / build-time
# Required to compile the optional ve_hpop pybind11 module that powers the
# --hpop High-Precision Orbit Propagator backend. Not needed for the default
# Skyfield/SGP4 path. Build:  cmake .. -DBUILD_PYTHON_BINDINGS=ON
pybind11>=2.13
